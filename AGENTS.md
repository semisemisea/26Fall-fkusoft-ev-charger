# 项目开发约束

## Qt 版本

- 项目统一使用 **Qt 6.2.4**；其他 Qt 版本不属于受支持的构建环境。
- 新增或修改 CMake 依赖时，使用 `find_package(Qt6 6.2.4 EXACT REQUIRED ...)`（或等效的版本校验），避免无意中依赖更高版本的 API。
- 只使用 Qt 6.2.4 已提供的 API。完成修改后，使用 Qt 6.2.4 配置并构建受影响的目标，确认版本和构建结果符合要求。

## 架构与实现

- 保持模块边界清晰：`biz-core` 放置可复用的业务规则与领域逻辑，`user-app` 和 `ops-app` 负责各自的界面与交互；依赖方向保持为应用层依赖核心层。
- 延续项目现有的 C++20、CMake 和 Qt 模式。优先使用 RAII、值语义、 const 正确性、窄接口以及 Qt 的信号与槽，令对象职责单一、依赖显式、生命周期可见。
- 在有需要时使用依赖注入；在项目复杂度达到足以引入依赖注入框架之前，采用手动构造并传递依赖的方式实现。
- 采用现代开发范式，但以可读性和可维护性为准；只有在确实降低复杂度或隔离变化时才引入新的抽象、框架或设计模式。

## 项目范围

- 这是小组作业项目，目标是清晰、可靠且易于协作的实现，不按工业级系统的可用性、扩展性或运维标准设计。
- 在满足功能和课程要求的前提下，选择简洁轻量的方案：避免不必要的分布式组件、复杂基础设施、过度工程化的错误处理、日志或配置体系。
- 若某项复杂度确有必要，应在代码结构或变更说明中明确它解决的问题，并保持实现范围与实际需求相称。

## 完成检查

- 修改 C++ 后遵循仓库的 `.clang-format`；构建并检查所有受影响的目标。
- 提交前确认没有引入 Qt 6.2.4 之后才支持的 API，也没有绕过模块边界或为课程需求之外的问题增加复杂度。
